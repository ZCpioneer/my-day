# 朝暮 V0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做出可侧载的安卓 App：朝/暮对话、确认后才写入的 Todo、当日日记、屏幕 Debug，手机单机连 DeepSeek。

**Architecture:** Vue 3 界面跑在 Capacitor 安卓壳里。聊天/待办/日记进 IndexedDB（库名 `zhaomu`）；Key/模型/调试开关进 Capacitor Preferences。Agent 是单循环：上下文 + DeepSeek Chat Completions + 四个工具。`propose_todos` 只弹确认框，由 UI 写入。真机请求走 CapacitorHttp（原生），禁止用浏览器 `fetch` 打 `api.deepseek.com`。

**Tech Stack:** Vue 3.5、Vite 6、TypeScript、Vitest、fake-indexeddb、@vue/test-utils、happy-dom、Capacitor 7、@capacitor/android、@capacitor/preferences、CapacitorHttp。

## Global Constraints

- 只打安卓 APK，不上架、不做 iOS。
- DeepSeek `https://api.deepseek.com`，`POST /chat/completions`，不流式，不开 thinking。
- 默认模型 `deepseek-v4-flash`，设置可改为 `deepseek-v4-pro`。
- Agent 永不直接写 Todo；没有「勾掉任务」工具。
- 朝/暮按钮只出现在对话页。
- 一轮用户发言最多 4 次模型调用。
- 调试面板第一版默认打开；日志不打印完整 API Key。
- 界面中文；视觉跟 `demo/index.html`（墨底、灯暖金、暮蓝）。
- IndexedDB 库名 `zhaomu`；不要 SQLite、不要自建服务器。
- `src/debug/default-key.ts` 头注释：`DEBUG ONLY，正式发布前删除`；仓库里只提交空字符串，真 Key 由作者本地填入且不要 commit。

---

## File Structure

```
package.json
vite.config.ts
vitest.config.ts
tsconfig.json
tsconfig.app.json
index.html
.gitignore
capacitor.config.ts
src/main.ts
src/App.vue
src/styles.css
src/types.ts
src/ids.ts
src/dates.ts
src/keys.ts
src/todos-filter.ts
src/debug/default-key.ts          # gitignored
src/debug/default-key.example.ts
src/debug/log.ts
src/storage/db.ts
src/storage/settings.ts
src/api/deepseek.ts
src/agent/prompt.ts
src/agent/tools.ts
src/agent/loop.ts
src/agent/context.ts
src/components/DebugPanel.vue
src/components/RitualBar.vue
src/components/TodoConfirm.vue
src/screens/ChatScreen.vue
src/screens/TodoScreen.vue
src/screens/DiaryScreen.vue
src/screens/SettingsScreen.vue
src/__tests__/dates.test.ts
src/__tests__/keys.test.ts
src/__tests__/todos-filter.test.ts
src/__tests__/log.test.ts
src/__tests__/db.test.ts
src/__tests__/settings.test.ts
src/__tests__/deepseek.test.ts
src/__tests__/loop.test.ts
src/__tests__/ritual.test.ts
src/__tests__/todos-screen.test.ts
```

职责：`types.ts` 全项目类型；`dates.ts`/`keys.ts`/`todos-filter.ts` 纯函数；`storage/` 持久化；`api/deepseek.ts` 只负责 HTTP 与错误文案；`agent/` 提示词、工具、循环；`screens/` 四页；`App.vue` 顶栏、底栏、朝暮显隐、Debug 浮层。

---

### Task 1: 工程脚手架与 Vitest

**Files:**
- Create: `package.json`
- Create: `vite.config.ts`
- Create: `vitest.config.ts`
- Create: `tsconfig.json`
- Create: `tsconfig.app.json`
- Create: `index.html`
- Create: `src/main.ts`
- Create: `src/App.vue`
- Create: `.gitignore`
- Create: `src/vite-env.d.ts`

**Interfaces:**
- Consumes: 无
- Produces: `npm test` 可跑；`npm run dev` 能打开空白页

- [ ] **Step 1: 写 package.json 与配置**

```json
{
  "name": "zhaomu",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "cap:sync": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0",
    "@capacitor/preferences": "^7.0.0",
    "vue": "^3.5.13"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "@types/node": "^22.13.0",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/test-utils": "^2.4.6",
    "fake-indexeddb": "^6.0.0",
    "happy-dom": "^17.1.0",
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5",
    "vue-tsc": "^2.2.0"
  }
}
```

`vite.config.ts`:

```ts
import { defineConfig } from "vite";
import vue from "@vitejs/plugin-vue";
import { fileURLToPath, URL } from "node:url";

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { "@": fileURLToPath(new URL("./src", import.meta.url)) },
  },
});
```

`vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";
import vue from "@vitejs/plugin-vue";
import { fileURLToPath, URL } from "node:url";

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: { "@": fileURLToPath(new URL("./src", import.meta.url)) },
  },
  test: {
    environment: "happy-dom",
    setupFiles: ["src/__tests__/setup.ts"],
  },
});
```

`src/__tests__/setup.ts`:

```ts
import "fake-indexeddb/auto";
```

`tsconfig.json`:

```json
{
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }]
}
```

`tsconfig.app.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "jsx": "preserve",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "paths": { "@/*": ["./src/*"] },
    "types": ["vite/client"],
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src/**/*.ts", "src/**/*.vue", "src/**/*.d.ts"]
}
```

`index.html`:

```html
<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
    <title>朝暮</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

`src/vite-env.d.ts`:

```ts
/// <reference types="vite/client" />
```

`src/main.ts`:

```ts
import { createApp } from "vue";
import App from "./App.vue";
createApp(App).mount("#app");
```

`src/App.vue`:

```vue
<template>
  <p>朝暮</p>
</template>
```

`.gitignore`:

```
node_modules
dist
android/app/build
android/.gradle
android/local.properties
.DS_Store
```

- [ ] **Step 2: 写一个占位测试证明 Vitest 能跑**

Create `src/__tests__/smoke.test.ts`:

```ts
import { describe, it, expect } from "vitest";

describe("scaffold", () => {
  it("runs", () => {
    expect(1 + 1).toBe(2);
  });
});
```

- [ ] **Step 3: 安装依赖并跑测试**

Run: `npm install && npm test`

Expected: PASS，`scaffold runs`

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json vite.config.ts vitest.config.ts tsconfig.json tsconfig.app.json index.html .gitignore src
git commit -m "chore: scaffold Vue 3, Vite, and Vitest"
```

---

### Task 2: 日期、Key、待办去重

**Files:**
- Create: `src/types.ts`
- Create: `src/dates.ts`
- Create: `src/ids.ts`
- Create: `src/keys.ts`
- Create: `src/todos-filter.ts`
- Create: `src/debug/default-key.example.ts`
- Create: `src/__tests__/dates.test.ts`
- Create: `src/__tests__/keys.test.ts`
- Create: `src/__tests__/todos-filter.test.ts`

**Interfaces:**
- Consumes: 无
- Produces:
  - `localDate(now?: Date): string` → `YYYY-MM-DD`（本地时区）
  - `newId(): string`
  - `resolveApiKey(settingsKey: string, defaultKey: string): string | null`
  - `maskKey(key: string): string`
  - `filterProposedTodos(proposed: ProposedTodo[], existing: Todo[]): ProposedTodo[]`
  - `DEFAULT_MODEL = "deepseek-v4-flash"`
  - types: `ChatMode`, `ChatMessage`, `DayChat`, `Todo`, `DailyLog`, `Settings`, `DebugEvent`, `DebugEntry`, `ProposedTodo`

- [ ] **Step 1: 写失败测试**

`src/__tests__/dates.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { localDate } from "@/dates";

describe("localDate", () => {
  it("formats the local calendar day", () => {
    const d = new Date(2026, 8, 5, 23, 30, 0);
    expect(localDate(d)).toBe("2026-09-05");
  });
});
```

`src/__tests__/keys.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { resolveApiKey, maskKey } from "@/keys";

describe("resolveApiKey", () => {
  it("prefers the settings key", () => {
    expect(resolveApiKey("sk-user", "sk-debug")).toBe("sk-user");
  });
  it("falls back to default", () => {
    expect(resolveApiKey("  ", "sk-debug")).toBe("sk-debug");
  });
  it("returns null when both empty", () => {
    expect(resolveApiKey("", "")).toBe(null);
  });
});

describe("maskKey", () => {
  it("keeps 4 prefix chars", () => {
    expect(maskKey("sk-abcdefghijk")).toBe("sk-a****");
  });
  it("masks short keys fully", () => {
    expect(maskKey("ab")).toBe("****");
  });
});
```

`src/__tests__/todos-filter.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { filterProposedTodos } from "@/todos-filter";
import type { Todo } from "@/types";

const existing: Todo[] = [
  {
    id: "1",
    title: "整理支付宝调试问题并发群里询问",
    status: "open",
    sourceDate: "2026-09-05",
    createdAt: "2026-09-05T01:00:00.000Z",
  },
];

describe("filterProposedTodos", () => {
  it("drops empty titles", () => {
    expect(filterProposedTodos([{ title: "  " }], [])).toEqual([]);
  });
  it("drops duplicates ignoring case and spaces", () => {
    const out = filterProposedTodos(
      [{ title: "整理支付宝调试问题并发群里询问" }, { title: " 给房东转水电费 " }],
      existing,
    );
    expect(out.map((x) => x.title)).toEqual(["给房东转水电费"]);
  });
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test`

Expected: FAIL，模块不存在

- [ ] **Step 3: 最小实现**

`src/types.ts`:

```ts
export type ChatMode = "morning" | "evening" | "chat";
export type ChatRole = "user" | "assistant" | "system";

export interface ChatMessage {
  id: string;
  role: ChatRole;
  content: string;
  createdAt: string;
  mode?: ChatMode;
}

export interface DayChat {
  date: string;
  messages: ChatMessage[];
}

export interface Todo {
  id: string;
  title: string;
  status: "open" | "done";
  sourceDate: string;
  createdAt: string;
  completedAt?: string;
  fromMessageId?: string;
}

export interface DailyLog {
  date: string;
  plan: string;
  done: string[];
  undone: string[];
  state: string;
  updatedAt: string;
}

export interface Settings {
  apiKey: string;
  model: string;
  debugOverlay: boolean;
}

export type DebugEvent =
  | "http_start"
  | "http_ok"
  | "http_fail"
  | "tool_call"
  | "tool_result"
  | "propose_ui"
  | "todo_confirmed"
  | "todo_rejected"
  | "log_written"
  | "agent_stop";

export interface DebugEntry {
  id: string;
  time: string;
  event: DebugEvent;
  detail: string;
  status?: number;
  durationMs?: number;
  model?: string;
  tool?: string;
}

export interface ProposedTodo {
  title: string;
  reason?: string;
}

export const DEFAULT_MODEL = "deepseek-v4-flash";
```

`src/dates.ts`:

```ts
export function localDate(now: Date = new Date()): string {
  const y = now.getFullYear();
  const m = String(now.getMonth() + 1).padStart(2, "0");
  const d = String(now.getDate()).padStart(2, "0");
  return `${y}-${m}-${d}`;
}
```

`src/ids.ts`:

```ts
export function newId(): string {
  return crypto.randomUUID();
}
```

`src/keys.ts`:

```ts
export function resolveApiKey(settingsKey: string, defaultKey: string): string | null {
  const a = settingsKey.trim();
  if (a) return a;
  const b = defaultKey.trim();
  if (b) return b;
  return null;
}

export function maskKey(key: string): string {
  const t = key.trim();
  if (t.length <= 4) return "****";
  return `${t.slice(0, 4)}****`;
}
```

`src/todos-filter.ts`:

```ts
import type { ProposedTodo, Todo } from "./types";

function norm(s: string): string {
  return s.replace(/\s+/g, "").toLowerCase();
}

export function filterProposedTodos(proposed: ProposedTodo[], existing: Todo[]): ProposedTodo[] {
  const seen = new Set(existing.map((t) => norm(t.title)));
  const out: ProposedTodo[] = [];
  for (const p of proposed) {
    const title = p.title.trim();
    if (!title) continue;
    const key = norm(title);
    if (seen.has(key)) continue;
    seen.add(key);
    out.push({ title, reason: p.reason?.trim() || undefined });
  }
  return out;
}
```

`src/debug/default-key.ts` 与 `src/debug/default-key.example.ts` 内容相同：

```ts
/** DEBUG ONLY，正式发布前删除。把 Key 填在下一行。填了之后不要 git add 这个文件。 */
export const DEFAULT_DEBUG_KEY = "";
```

- [ ] **Step 4: 跑测试**

Run: `npm test`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/types.ts src/dates.ts src/ids.ts src/keys.ts src/todos-filter.ts src/debug src/__tests__
git commit -m "feat: add date, API key, and todo-dedupe helpers"
```

---

### Task 3: 屏幕调试日志存储

**Files:**
- Create: `src/debug/log.ts`
- Create: `src/__tests__/log.test.ts`

**Interfaces:**
- Consumes: `DebugEntry`, `DebugEvent`, `newId`
- Produces:
  - `debugLog: { entries: DebugEntry[]; push(e: Omit<DebugEntry,"id"|"time">): void; clear(): void; toText(): string }`
  - `MAX_DEBUG_DETAIL = 500`

- [ ] **Step 1: 写失败测试**

`src/__tests__/log.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { debugLog } from "@/debug/log";

describe("debugLog", () => {
  beforeEach(() => debugLog.clear());

  it("stores events with time", () => {
    debugLog.push({ event: "http_fail", detail: "timeout", status: 0 });
    expect(debugLog.entries).toHaveLength(1);
    expect(debugLog.entries[0].event).toBe("http_fail");
    expect(debugLog.entries[0].status).toBe(0);
    expect(debugLog.entries[0].time.length).toBeGreaterThan(0);
  });

  it("truncates long detail", () => {
    debugLog.push({ event: "http_ok", detail: "x".repeat(800) });
    expect(debugLog.entries[0].detail.length).toBe(500);
  });

  it("toText is copyable", () => {
    debugLog.push({ event: "tool_call", detail: "propose_todos", tool: "propose_todos" });
    expect(debugLog.toText()).toContain("propose_todos");
  });
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test -- src/__tests__/log.test.ts`

Expected: FAIL，模块不存在

- [ ] **Step 3: 实现**

`src/debug/log.ts`:

```ts
import { reactive } from "vue";
import { newId } from "../ids";
import type { DebugEntry } from "../types";

export const MAX_DEBUG_DETAIL = 500;

function clip(s: string): string {
  return s.length > MAX_DEBUG_DETAIL ? s.slice(0, MAX_DEBUG_DETAIL) : s;
}

export const debugLog = reactive({
  entries: [] as DebugEntry[],
  push(e: Omit<DebugEntry, "id" | "time">) {
    this.entries.push({
      id: newId(),
      time: new Date().toISOString(),
      ...e,
      detail: clip(e.detail),
    });
  },
  clear() {
    this.entries.splice(0, this.entries.length);
  },
  toText(): string {
    return this.entries
      .map((e) => {
        const bits = [e.time, e.event];
        if (e.status != null) bits.push(`status=${e.status}`);
        if (e.durationMs != null) bits.push(`${e.durationMs}ms`);
        if (e.model) bits.push(e.model);
        if (e.tool) bits.push(e.tool);
        bits.push(e.detail);
        return bits.join(" | ");
      })
      .join("\n");
  },
});
```

- [ ] **Step 4: 跑测试**

Run: `npm test -- src/__tests__/log.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/debug/log.ts src/__tests__/log.test.ts
git commit -m "feat: add in-memory debug log used by the on-screen panel"
```

---

### Task 4: IndexedDB（对话、待办、日记）

**Files:**
- Create: `src/storage/db.ts`
- Create: `src/__tests__/db.test.ts`

**Interfaces:**
- Consumes: `DayChat`, `ChatMessage`, `Todo`, `DailyLog`, `localDate`, `newId`
- Produces:
  - `openDb(): Promise<IDBDatabase>`
  - `chatRepo.get(date: string): Promise<DayChat>`
  - `chatRepo.append(date: string, msg: ChatMessage): Promise<void>`
  - `todoRepo.list(): Promise<Todo[]>`
  - `todoRepo.add(todo: Todo): Promise<void>`
  - `todoRepo.toggle(id: string, now?: Date): Promise<Todo>`
  - `logRepo.get(date: string): Promise<DailyLog | null>`
  - `logRepo.put(log: DailyLog): Promise<void>`
  - 库名 `zhaomu`，version `1`，stores: `chats` keyPath `date`；`todos` keyPath `id`；`logs` keyPath `date`

- [ ] **Step 1: 写失败测试**

`src/__tests__/db.test.ts`:

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { chatRepo, todoRepo, logRepo, deleteDb } from "@/storage/db";
import type { ChatMessage, Todo, DailyLog } from "@/types";

beforeEach(async () => {
  await deleteDb();
});

describe("chatRepo", () => {
  it("starts a day empty and appends", async () => {
    const empty = await chatRepo.get("2026-09-05");
    expect(empty.messages).toEqual([]);
    const msg: ChatMessage = {
      id: "m1",
      role: "user",
      content: "开始今天。",
      createdAt: "2026-09-05T01:00:00.000Z",
      mode: "morning",
    };
    await chatRepo.append("2026-09-05", msg);
    const day = await chatRepo.get("2026-09-05");
    expect(day.messages).toHaveLength(1);
    expect(day.messages[0].content).toBe("开始今天。");
  });
});

describe("todoRepo", () => {
  it("adds and toggles without dropping other days", async () => {
    const a: Todo = {
      id: "t1",
      title: "给房东转水电费",
      status: "open",
      sourceDate: "2026-09-04",
      createdAt: "2026-09-04T01:00:00.000Z",
    };
    await todoRepo.add(a);
    const done = await todoRepo.toggle("t1", new Date("2026-09-05T12:00:00.000Z"));
    expect(done.status).toBe("done");
    expect(done.completedAt).toBe("2026-09-05T12:00:00.000Z");
    const listed = await todoRepo.list();
    expect(listed).toHaveLength(1);
  });
});

describe("logRepo", () => {
  it("overwrites the same date", async () => {
    const first: DailyLog = {
      date: "2026-09-05",
      plan: "先调试",
      done: [],
      undone: ["周报"],
      state: "烦",
      updatedAt: "2026-09-05T12:00:00.000Z",
    };
    await logRepo.put(first);
    await logRepo.put({ ...first, state: "还行", updatedAt: "2026-09-05T13:00:00.000Z" });
    const got = await logRepo.get("2026-09-05");
    expect(got?.state).toBe("还行");
  });
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test -- src/__tests__/db.test.ts`

Expected: FAIL

- [ ] **Step 3: 实现**

`src/storage/db.ts` 用 Promise 包一层 IDB：

```ts
import type { ChatMessage, DailyLog, DayChat, Todo } from "../types";

const DB_NAME = "zhaomu";
const DB_VERSION = 1;

function openDb(): Promise<IDBDatabase> {
  return new Promise((resolve, reject) => {
    const req = indexedDB.open(DB_NAME, DB_VERSION);
    req.onupgradeneeded = () => {
      const db = req.result;
      if (!db.objectStoreNames.contains("chats")) db.createObjectStore("chats", { keyPath: "date" });
      if (!db.objectStoreNames.contains("todos")) db.createObjectStore("todos", { keyPath: "id" });
      if (!db.objectStoreNames.contains("logs")) db.createObjectStore("logs", { keyPath: "date" });
    };
    req.onsuccess = () => resolve(req.result);
    req.onerror = () => reject(req.error);
  });
}

function txDone(tx: IDBTransaction): Promise<void> {
  return new Promise((resolve, reject) => {
    tx.oncomplete = () => resolve();
    tx.onerror = () => reject(tx.error);
    tx.onabort = () => reject(tx.error);
  });
}

export async function deleteDb(): Promise<void> {
  await new Promise<void>((resolve, reject) => {
    const req = indexedDB.deleteDatabase(DB_NAME);
    req.onsuccess = () => resolve();
    req.onerror = () => reject(req.error);
    req.onblocked = () => resolve();
  });
}

export const chatRepo = {
  async get(date: string): Promise<DayChat> {
    const db = await openDb();
    const row = await new Promise<DayChat | undefined>((resolve, reject) => {
      const req = db.transaction("chats").objectStore("chats").get(date);
      req.onsuccess = () => resolve(req.result as DayChat | undefined);
      req.onerror = () => reject(req.error);
    });
    db.close();
    return row ?? { date, messages: [] };
  },
  async append(date: string, msg: ChatMessage): Promise<void> {
    const db = await openDb();
    const tx = db.transaction("chats", "readwrite");
    const store = tx.objectStore("chats");
    const current = await new Promise<DayChat>((resolve, reject) => {
      const req = store.get(date);
      req.onsuccess = () => resolve((req.result as DayChat) ?? { date, messages: [] });
      req.onerror = () => reject(req.error);
    });
    current.messages.push(msg);
    store.put(current);
    await txDone(tx);
    db.close();
  },
};

export const todoRepo = {
  async list(): Promise<Todo[]> {
    const db = await openDb();
    const rows = await new Promise<Todo[]>((resolve, reject) => {
      const req = db.transaction("todos").objectStore("todos").getAll();
      req.onsuccess = () => resolve(req.result as Todo[]);
      req.onerror = () => reject(req.error);
    });
    db.close();
    return rows;
  },
  async add(todo: Todo): Promise<void> {
    const db = await openDb();
    const tx = db.transaction("todos", "readwrite");
    tx.objectStore("todos").put(todo);
    await txDone(tx);
    db.close();
  },
  async toggle(id: string, now: Date = new Date()): Promise<Todo> {
    const db = await openDb();
    const tx = db.transaction("todos", "readwrite");
    const store = tx.objectStore("todos");
    const todo = await new Promise<Todo>((resolve, reject) => {
      const req = store.get(id);
      req.onsuccess = () => resolve(req.result as Todo);
      req.onerror = () => reject(req.error);
    });
    if (!todo) throw new Error("todo not found");
    if (todo.status === "open") {
      todo.status = "done";
      todo.completedAt = now.toISOString();
    } else {
      todo.status = "open";
      delete todo.completedAt;
    }
    store.put(todo);
    await txDone(tx);
    db.close();
    return todo;
  },
};

export const logRepo = {
  async get(date: string): Promise<DailyLog | null> {
    const db = await openDb();
    const row = await new Promise<DailyLog | undefined>((resolve, reject) => {
      const req = db.transaction("logs").objectStore("logs").get(date);
      req.onsuccess = () => resolve(req.result as DailyLog | undefined);
      req.onerror = () => reject(req.error);
    });
    db.close();
    return row ?? null;
  },
  async put(log: DailyLog): Promise<void> {
    const db = await openDb();
    const tx = db.transaction("logs", "readwrite");
    tx.objectStore("logs").put(log);
    await txDone(tx);
    db.close();
  },
};
```

- [ ] **Step 4: 跑测试**

Run: `npm test -- src/__tests__/db.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/storage/db.ts src/__tests__/db.test.ts
git commit -m "feat: persist chats, todos, and daily logs in IndexedDB"
```

---

### Task 5: 设置（Preferences + 默认 Key）

**Files:**
- Create: `src/storage/settings.ts`
- Create: `src/__tests__/settings.test.ts`

**Interfaces:**
- Consumes: `Settings`, `DEFAULT_MODEL`, `resolveApiKey`
- Produces:
  - `loadSettings(): Promise<Settings>`
  - `saveSettings(s: Settings): Promise<void>`
  - `effectiveApiKey(s: Settings): string | null`（内部读 `DEFAULT_DEBUG_KEY`）
  - 测试通过注入 `prefs: { get/set }`，不要在单测里碰真 Capacitor

实现用可选 `prefs` 参数，生产默认走 `@capacitor/preferences`。在 Node 测试里传入内存 map。Web 无插件时也用内存/localStorage 回退：`localStorage` key `zhaomu.settings`。

- [ ] **Step 1: 写失败测试**

```ts
import { describe, it, expect } from "vitest";
import { loadSettings, saveSettings, effectiveApiKey } from "@/storage/settings";
import { DEFAULT_MODEL } from "@/types";

const mem = new Map<string, string>();
const prefs = {
  async get(k: string) {
    return mem.get(k) ?? null;
  },
  async set(k: string, v: string) {
    mem.set(k, v);
  },
};

describe("settings", () => {
  it("defaults model and debug overlay", async () => {
    mem.clear();
    const s = await loadSettings(prefs);
    expect(s.model).toBe(DEFAULT_MODEL);
    expect(s.debugOverlay).toBe(true);
    expect(s.apiKey).toBe("");
  });

  it("roundtrips", async () => {
    mem.clear();
    await saveSettings({ apiKey: "sk-user", model: "deepseek-v4-pro", debugOverlay: false }, prefs);
    const s = await loadSettings(prefs);
    expect(s.apiKey).toBe("sk-user");
    expect(s.model).toBe("deepseek-v4-pro");
    expect(s.debugOverlay).toBe(false);
  });
});

describe("effectiveApiKey", () => {
  it("uses settings then default", () => {
    expect(effectiveApiKey({ apiKey: "", model: DEFAULT_MODEL, debugOverlay: true }, "sk-debug")).toBe(
      "sk-debug",
    );
  });
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test -- src/__tests__/settings.test.ts`

Expected: FAIL

- [ ] **Step 3: 实现**

`src/storage/settings.ts`:

```ts
import { DEFAULT_MODEL, type Settings } from "../types";
import { resolveApiKey } from "../keys";
import { DEFAULT_DEBUG_KEY } from "../debug/default-key";

const KEY = "zhaomu.settings";

export interface Prefs {
  get(key: string): Promise<string | null>;
  set(key: string, value: string): Promise<void>;
}

const localPrefs: Prefs = {
  async get(key) {
    return localStorage.getItem(key);
  },
  async set(key, value) {
    localStorage.setItem(key, value);
  },
};

export async function loadSettings(prefs: Prefs = localPrefs): Promise<Settings> {
  const raw = await prefs.get(KEY);
  if (!raw) return { apiKey: "", model: DEFAULT_MODEL, debugOverlay: true };
  const parsed = JSON.parse(raw) as Partial<Settings>;
  return {
    apiKey: parsed.apiKey ?? "",
    model: parsed.model || DEFAULT_MODEL,
    debugOverlay: parsed.debugOverlay ?? true,
  };
}

export async function saveSettings(s: Settings, prefs: Prefs = localPrefs): Promise<void> {
  await prefs.set(KEY, JSON.stringify(s));
}

export function effectiveApiKey(s: Settings, fallback: string = DEFAULT_DEBUG_KEY): string | null {
  return resolveApiKey(s.apiKey, fallback);
}
```

`src/debug/default-key.ts` 在 Task 2 已提交空字符串。本任务从该文件 import。作者填入真 Key 后不要 `git add` 该文件。

- [ ] **Step 4: 跑测试**

Run: `npm test -- src/__tests__/settings.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/storage/settings.ts src/__tests__/settings.test.ts src/debug/default-key.ts .gitignore
git commit -m "feat: load and save settings with debug API key fallback"
```

---

### Task 6: DeepSeek HTTP 客户端

**Files:**
- Create: `src/api/deepseek.ts`
- Create: `src/__tests__/deepseek.test.ts`

**Interfaces:**
- Consumes: `debugLog`, `maskKey`
- Produces:
```ts
export interface ToolDef {
  type: "function";
  function: { name: string; description: string; parameters: unknown };
}
export interface ToolCall {
  id: string;
  type: "function";
  function: { name: string; arguments: string };
}
export interface ApiMessage {
  role: "system" | "user" | "assistant" | "tool";
  content: string | null;
  tool_call_id?: string;
  tool_calls?: ToolCall[];
}
export interface ChatCompletionRequest {
  model: string;
  messages: ApiMessage[];
  tools: ToolDef[];
  tool_choice?: "auto";
  stream: false;
}
export interface ChatCompletionResponse {
  content: string | null;
  tool_calls: ToolCall[];
}
export class ApiError extends Error {
  constructor(
    message: string,
    public status: number,
    public userMessage: string,
  ) { super(message); }
}
export type PostJson = (url: string, headers: Record<string, string>, body: unknown) => Promise<{ status: number; json: unknown }>;
export function chatCompletions(args: {
  apiKey: string;
  request: ChatCompletionRequest;
  postJson: PostJson;
}): Promise<ChatCompletionResponse>;
export function userMessageForStatus(status: number): string;
```

`userMessageForStatus`：401 → `Key 不对或没有额度`；0/网络 → `现在没网，过一会儿再聊。待办还可以勾`；其它 → `模型这轮没回上，再说一次`

- [ ] **Step 1: 写失败测试**

```ts
import { describe, it, expect } from "vitest";
import { chatCompletions, userMessageForStatus, ApiError } from "@/api/deepseek";
import { debugLog } from "@/debug/log";

describe("userMessageForStatus", () => {
  it("maps 401", () => {
    expect(userMessageForStatus(401)).toBe("Key 不对或没有额度");
  });
});

describe("chatCompletions", () => {
  it("posts to official URL and logs", async () => {
    debugLog.clear();
    const postJson = async (url: string, headers: Record<string, string>, body: unknown) => {
      expect(url).toBe("https://api.deepseek.com/chat/completions");
      expect(headers.Authorization).toBe("Bearer sk-test");
      expect((body as { stream: boolean }).stream).toBe(false);
      return {
        status: 200,
        json: {
          choices: [{ message: { content: "早。", tool_calls: undefined } }],
        },
      };
    };
    const res = await chatCompletions({
      apiKey: "sk-test",
      request: { model: "deepseek-v4-flash", messages: [], tools: [], stream: false },
      postJson,
    });
    expect(res.content).toBe("早。");
    expect(debugLog.entries.some((e) => e.event === "http_ok")).toBe(true);
    expect(debugLog.toText()).not.toContain("sk-test");
  });

  it("throws ApiError on 401", async () => {
    debugLog.clear();
    const postJson = async () => ({ status: 401, json: { error: { message: "invalid" } } });
    await expect(
      chatCompletions({
        apiKey: "sk-bad",
        request: { model: "deepseek-v4-flash", messages: [], tools: [], stream: false },
        postJson,
      }),
    ).rejects.toBeInstanceOf(ApiError);
    expect(debugLog.entries.some((e) => e.event === "http_fail")).toBe(true);
  });
});
```

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test -- src/__tests__/deepseek.test.ts`

Expected: FAIL

- [ ] **Step 3: 实现**（`chatCompletions` 必须 `debugLog.push({event:"http_start"})`，成功 `http_ok`，失败 `http_fail`；detail 用 `maskKey(apiKey)` 而不是全文）

```ts
export function userMessageForStatus(status: number): string {
  if (status === 401 || status === 402 || status === 403) return "Key 不对或没有额度";
  if (status === 0) return "现在没网，过一会儿再聊。待办还可以勾";
  return "模型这轮没回上，再说一次";
}
```

解析 `choices[0].message.content` 与 `tool_calls`（缺省 `[]`）。非 2xx 抛 `new ApiError(raw, status, userMessageForStatus(status))`。

- [ ] **Step 4: 跑测试**

Run: `npm test -- src/__tests__/deepseek.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/api/deepseek.ts src/__tests__/deepseek.test.ts
git commit -m "feat: add DeepSeek chat client with debug logging"
```

---

### Task 7: Agent 工具与循环

**Files:**
- Create: `src/agent/prompt.ts`
- Create: `src/agent/tools.ts`
- Create: `src/agent/context.ts`
- Create: `src/agent/loop.ts`
- Create: `src/__tests__/loop.test.ts`

**Interfaces:**
- Consumes: 前面全部类型与 `chatCompletions` 的 request/response、`filterProposedTodos`、`debugLog`、`localDate`
- Produces:
```ts
export const MAX_MODEL_CALLS = 4;
export const TOOL_DEFS: ToolDef[]; // list_todos, propose_todos, suggest_order, write_daily_log
export function systemPrompt(): string;
export function buildContextMessages(input: {
  date: string;
  timeLabel: string;
  mode: ChatMode;
  openTodos: Todo[];
  doneToday: Todo[];
  messages: ChatMessage[];
}): ApiMessage[];
export interface AgentDeps {
  complete: (req: ChatCompletionRequest) => Promise<ChatCompletionResponse>;
  listTodos: () => Promise<Todo[]>;
  addTodos: (items: ProposedTodo[]) => Promise<void>;
  writeDailyLog: (log: DailyLog) => Promise<void>;
  now: () => Date;
  onPropose: (items: ProposedTodo[]) => Promise<ProposedTodo[]>;
}
export function runAgent(input: {
  deps: AgentDeps;
  mode: ChatMode;
  userText: string;
  history: ChatMessage[];
  model: string;
}): Promise<{ assistantText: string; stopped: boolean }>;
```

`onPropose`：UI 弹框；返回用户勾选的项（空数组 = 这次不加）。循环里 `propose_todos` **先 filter，再 onPropose，再 addTodos（只对返回项）**。工具函数本身不直接碰 IDB。

`write_daily_log` 参数：`plan: string, done: string[], undone: string[], state: string`。用 `deps.now` 填 `date`/`updatedAt` 后 `writeDailyLog`。失败则把错误字符串作为 tool result，**不得部分写入**（`writeDailyLog` 要么整篇 put 成功）。

`suggest_order` 参数 `order: string[]`（标题顺序），tool result 为「已记下建议顺序：…」。不改 todos 数组顺序。

截断对话：`buildContextMessages` 保留全部 `mode === "morning"|"evening"` 的消息，外加最近 16 条；总消息超过 40 条时丢弃最老的闲聊。

`systemPrompt()` 全文：

```
你是「朝暮」，用户的个人日环助手。你帮助他回忆、排优先级、抽出可执行的小事。你不是人生教练，也不要把一个想法升级成新项目。

规则：
- 每次只问一个问题，短，像聊天。
- 不要声称已经写入待办。只能提议，等用户在确认框里点。
- 没有「勾掉任务」的能力。完成情况以工具读到的列表为准。
- 朝：先报还没做完的件数和标题，再问今天怎么安排，再给一个顺序建议。
- 暮：先对照做成了/没做完，问怎么了；给一个能立刻变小的改法，并安慰一句。收束时调用 write_daily_log。
- 闲聊：纯情绪不提议任务；出现明确要做的动作才 propose_todos。
```

- [ ] **Step 1: 写失败测试（假 complete）**

```ts
import { describe, it, expect } from "vitest";
import { runAgent, MAX_MODEL_CALLS } from "@/agent/loop";
import type { AgentDeps } from "@/agent/loop";
import type { ChatCompletionRequest, ChatCompletionResponse } from "@/api/deepseek";
import type { ProposedTodo, Todo } from "@/types";

function deps(over: Partial<AgentDeps> & { complete: AgentDeps["complete"] }): AgentDeps {
  return {
    listTodos: async () => [],
    addTodos: async () => {},
    writeDailyLog: async () => {},
    now: () => new Date(2026, 8, 5, 8, 0, 0),
    onPropose: async (items) => items,
    ...over,
  };
}

describe("runAgent", () => {
  it("returns assistant text when no tools", async () => {
    const d = deps({
      complete: async () => ({ content: "早。昨天还剩 2 件。", tool_calls: [] }),
    });
    const r = await runAgent({ deps: d, mode: "morning", userText: "开始今天。", history: [], model: "deepseek-v4-flash" });
    expect(r.assistantText).toContain("早。");
    expect(r.stopped).toBe(false);
  });

  it("does not write todos until onPropose returns them", async () => {
    const added: string[] = [];
    let calls = 0;
    const d = deps({
      complete: async (req: ChatCompletionRequest): Promise<ChatCompletionResponse> => {
        calls += 1;
        if (calls === 1) {
          return {
            content: null,
            tool_calls: [
              {
                id: "c1",
                type: "function",
                function: {
                  name: "propose_todos",
                  arguments: JSON.stringify({ items: [{ title: "给房东转水电费" }] }),
                },
              },
            ],
          };
        }
        return { content: "已记下要加的。", tool_calls: [] };
      },
      onPropose: async () => [] as ProposedTodo[],
      addTodos: async (items) => {
        added.push(...items.map((i) => i.title));
      },
    });
    const r = await runAgent({ deps: d, mode: "chat", userText: "还要给房东转水电费", history: [], model: "deepseek-v4-flash" });
    expect(added).toEqual([]);
    expect(r.assistantText.length).toBeGreaterThan(0);
  });

  it("stops after MAX_MODEL_CALLS", async () => {
    const d = deps({
      complete: async () => ({
        content: null,
        tool_calls: [
          {
            id: "x",
            type: "function",
            function: { name: "list_todos", arguments: "{}" },
          },
        ],
      }),
    });
    const r = await runAgent({ deps: d, mode: "chat", userText: "hi", history: [], model: "deepseek-v4-flash" });
    expect(r.stopped).toBe(true);
    expect(r.assistantText).toContain("这轮没做成");
  });

  it("write_daily_log persists one full document", async () => {
    const saved: string[] = [];
    let calls = 0;
    const d = deps({
      complete: async (): Promise<ChatCompletionResponse> => {
        calls += 1;
        if (calls === 1) {
          return {
            content: null,
            tool_calls: [
              {
                id: "w",
                type: "function",
                function: {
                  name: "write_daily_log",
                  arguments: JSON.stringify({
                    plan: "先调试",
                    done: ["支付宝"],
                    undone: ["周报"],
                    state: "下午有点烦",
                  }),
                },
              },
            ],
          };
        }
        return { content: "日记写下了。", tool_calls: [] };
      },
      writeDailyLog: async (log) => {
        saved.push(log.state);
      },
    });
    await runAgent({ deps: d, mode: "evening", userText: "今天结束了。", history: [], model: "deepseek-v4-flash" });
    expect(saved).toEqual(["下午有点烦"]);
  });
});
```

把 `MAX_MODEL_CALLS` 从 `@/agent/loop` 导出；超限文案必须含 `这轮没做成`。

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test -- src/__tests__/loop.test.ts`

Expected: FAIL

- [ ] **Step 3: 实现 prompt.ts / tools.ts / context.ts / loop.ts**

`tools.ts` 导出 `TOOL_DEFS` 四个 function，JSON Schema：

- `list_todos` 无参数
- `propose_todos`：`{ items: { title: string, reason?: string }[] }`
- `suggest_order`：`{ order: string[] }`
- `write_daily_log`：`{ plan: string, done: string[], undone: string[], state: string }`

`loop.ts` 逻辑：

1. `messages = [system, ...buildContextMessages(...), {role:"user", content: userText}]`
2. for i in 1..MAX_MODEL_CALLS:
   - `res = await complete({ model, messages, tools: TOOL_DEFS, tool_choice: "auto", stream: false })`
   - 若无 tool_calls：返回 `res.content || ""`
   - 否则对每个 tool_call：`debugLog.push tool_call` → execute → `debugLog.push tool_result` → `messages.push assistant+tool`
3. 超限：`debugLog.push agent_stop`，返回「这轮没做成，再说一次。」

`propose_todos` execute：parse items → `filterProposedTodos` → 空则 tool result「没有可新增的任务」→ 否则 `debugLog propose_ui` → `accepted = await onPropose(valid)` → 空则 `todo_rejected` 且 result「用户这次不加」→ 否则 `await addTodos(accepted)`、`todo_confirmed`、result「用户已加入：…」

未知工具名：result「未知工具」。JSON 解析失败：result「参数无效」。

- [ ] **Step 4: 跑测试**

Run: `npm test -- src/__tests__/loop.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/agent src/__tests__/loop.test.ts
git commit -m "feat: add single-agent tool loop with confirm-before-write todos"
```

---

### Task 8: 应用壳 + 朝暮仅对话页

**Files:**
- Create: `src/styles.css`（从 `demo/index.html` 移植 CSS 变量、phone 布局、底栏、仪式按钮、气泡、确认框、Debug 面板；去掉 demo 的预写剧本）
- Modify: `src/main.ts` 引入 `styles.css`
- Modify: `src/App.vue`
- Create: `src/components/RitualBar.vue`
- Create: `src/__tests__/ritual.test.ts`

**Interfaces:**
- Consumes: 无新函数
- Produces: `App.vue` 状态 `tab: "chat" | "todo" | "diary" | "settings"`；`RitualBar` 只在 `tab==="chat"` 时渲染；顶栏「朝暮」+ 本地日期 + 齿轮进 settings

- [ ] **Step 1: 写失败测试**

```ts
import { describe, it, expect } from "vitest";
import { mount } from "@vue/test-utils";
import App from "@/App.vue";

describe("ritual visibility", () => {
  it("shows 朝/暮 only on chat tab", async () => {
    const w = mount(App);
    expect(w.text()).toContain("开始今天");
    expect(w.text()).toContain("回顾今天");
    await w.get("[data-nav=todo]").trigger("click");
    expect(w.text()).not.toContain("开始今天");
    await w.get("[data-nav=chat]").trigger("click");
    expect(w.text()).toContain("开始今天");
  });
});
```

此测试会在 ChatScreen 尚未完成时失败。先让 App 内联三个空屏 + RitualBar，ChatScreen 可先做占位，只要含 RitualBar。

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test -- src/__tests__/ritual.test.ts`

Expected: FAIL（没有 data-nav）

- [ ] **Step 3: 实现 RitualBar 与 App 底栏**

`RitualBar.vue`：两个按钮，文案「朝 / 开始今天」「暮 / 回顾今天」，emit `morning` / `evening`。

`App.vue` 结构：

```vue
<template>
  <div class="phone">
    <header>
      <div class="brand">朝暮</div>
      <div>
        <span>{{ dateLabel }}</span>
        <button type="button" data-nav="settings" @click="tab = 'settings'">设置</button>
      </div>
    </header>
    <RitualBar v-if="tab === 'chat'" @morning="onMorning" @evening="onEvening" />
    <ChatScreen v-if="tab === 'chat'" />
    <TodoScreen v-else-if="tab === 'todo'" />
    <DiaryScreen v-else-if="tab === 'diary'" />
    <SettingsScreen v-else />
    <nav>
      <button data-nav="chat" type="button" @click="tab = 'chat'">对话</button>
      <button data-nav="todo" type="button" @click="tab = 'todo'">待办</button>
      <button data-nav="diary" type="button" @click="tab = 'diary'">日记</button>
    </nav>
    <DebugPanel v-if="settings.debugOverlay" />
  </div>
</template>
```

本任务 Chat/Todo/Diary/Settings/Debug 可以是几乎空的单文件（一行字），只要测试通过。视觉 token 写进 `styles.css`。

`onMorning`/`onEvening` 本任务可 `console` 空函数，下一任务再接 Agent。

- [ ] **Step 4: 跑测试**

Run: `npm test -- src/__tests__/ritual.test.ts`

Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/App.vue src/styles.css src/main.ts src/components/RitualBar.vue src/screens src/__tests__/ritual.test.ts
git commit -m "feat: show morning/evening rituals only on the chat tab"
```

---

### Task 9: 对话、确认框、待办页、日记页、设置、Debug 面板

**Files:**
- Create: `src/screens/ChatScreen.vue`
- Create: `src/screens/TodoScreen.vue`
- Create: `src/screens/DiaryScreen.vue`
- Create: `src/screens/SettingsScreen.vue`
- Create: `src/components/TodoConfirm.vue`
- Create: `src/components/DebugPanel.vue`
- Modify: `src/App.vue`（把 Agent 与存储接上）
- Create: `src/__tests__/todos-screen.test.ts`

**Interfaces:**
- Consumes: `runAgent`, `chatRepo`, `todoRepo`, `logRepo`, `loadSettings`, `saveSettings`, `effectiveApiKey`, `chatCompletions`, `debugLog`, `localDate`, `newId`
- Produces: 可在浏览器完成规格第 11 节除 APK 外的路径（用 vite + 真 Key 或 mock）

`App.vue` 提供/持有：

```ts
const settings = ref(await loadSettings());
const todos = ref(await todoRepo.list());
const date = localDate();
const chat = ref(await chatRepo.get(date));
const daily = ref(await logRepo.get(date));
const pendingPropose = ref<ProposedTodo[] | null>(null);
let proposeResolve: ((v: ProposedTodo[]) => void) | null = null;
```

`postJson` 本任务用 `fetch`（仅电脑 `npm run dev` 调试）。Task 10 换成 CapacitorHttp。抽 `src/api/post-json.ts`：

```ts
export const browserPostJson: PostJson = async (url, headers, body) => {
  try {
    const res = await fetch(url, { method: "POST", headers: { "Content-Type": "application/json", ...headers }, body: JSON.stringify(body) });
    const json = await res.json().catch(() => ({}));
    return { status: res.status, json };
  } catch {
    return { status: 0, json: {} };
  }
};
```

`complete`：

```ts
async function complete(req: ChatCompletionRequest) {
  const key = effectiveApiKey(settings.value);
  if (!key) throw new ApiError("no key", 401, "去设置里粘贴 DeepSeek API Key");
  return chatCompletions({ apiKey: key, request: req, postJson: browserPostJson });
}
```

`onPropose`：设 `pendingPropose`，返回 Promise，等 `TodoConfirm` 点确认/不加。确认：`todoRepo.add` 每条（`status:"open"`, `sourceDate: localDate()`），刷新 `todos`。

朝：`userText = "开始今天。"`，`mode: "morning"`。暮：`userText = "今天结束了。"`, `mode: "evening"`。输入框：`mode: "chat"`。

无 Key：在对话里插入一条 assistant「去设置里粘贴 DeepSeek API Key」，`debugLog http_fail`。

`ApiError`：assistant 用 `userMessage`。本地数据不丢。

`TodoScreen`：未完成 / 已完成两区，点击调用 `todoRepo.toggle`。

`DiaryScreen`：无日志显示「晚上点「暮」聊完之后，今天的记录会出现在这里。」有则展示 plan/done/undone/state。

`SettingsScreen`：API Key 输入（type password）、模型 select（flash/pro）、debugOverlay 开关、保存。

`DebugPanel`：列出 `debugLog.entries`，按钮清空、复制 `debugLog.toText()`。默认展开。

- [ ] **Step 1: 待办页测试**

```ts
import { describe, it, expect, beforeEach } from "vitest";
import { mount } from "@vue/test-utils";
import TodoScreen from "@/screens/TodoScreen.vue";
import { todoRepo, deleteDb } from "@/storage/db";

beforeEach(async () => {
  await deleteDb();
  await todoRepo.add({
    id: "t1",
    title: "给房东转水电费",
    status: "open",
    sourceDate: "2026-09-05",
    createdAt: "2026-09-05T01:00:00.000Z",
  });
});

describe("TodoScreen", () => {
  it("toggles a row into 已经勾掉", async () => {
    const w = mount(TodoScreen, {
      props: {
        todos: await todoRepo.list(),
      },
    });
    expect(w.text()).toContain("给房东转水电费");
    await w.get("[data-todo=t1]").trigger("click");
    // parent handles toggle in App; for unit test, TodoScreen emits toggle
    expect(w.emitted("toggle")?.[0]).toEqual(["t1"]);
  });
});
```

`TodoScreen` 必须 `emit('toggle', id)`，`data-todo` 为 todo id。App 监听后 `todoRepo.toggle`。

- [ ] **Step 2: 跑测试，确认失败**

Run: `npm test -- src/__tests__/todos-screen.test.ts`

Expected: FAIL

- [ ] **Step 3: 实现四个 screen、TodoConfirm、DebugPanel，并接到 App**

朝/暮点击后禁用重复进入直到本轮 `runAgent` 结束（`awaiting` 标志）。

把用户与助手消息 `chatRepo.append`，刷新 thread。

- [ ] **Step 4: 跑全部测试**

Run: `npm test`

Expected: PASS

- [ ] **Step 5: 浏览器手测（电脑）**

Run: `npm run dev`

Expected: 三页可切；朝暮只在对话；无 Key 时有中文提示；有 Key 时朝能开口（作者填 default-key 或设置）

- [ ] **Step 6: Commit**

```bash
git add src
git commit -m "feat: wire chat, todos, diary, settings, and debug overlay"
```

---

### Task 10: Capacitor 安卓壳 + 原生 HTTP + 打 APK

**Files:**
- Create: `capacitor.config.ts`
- Create: `src/api/post-json.ts`（若 Task 9 已建则 Modify：原生分支）
- Modify: `src/storage/settings.ts` 在原生上用 `@capacitor/preferences`
- Modify: `src/App.vue` 使用 `nativePostJson` when `Capacitor.isNativePlatform()`

**Interfaces:**
- Consumes: `PostJson`
- Produces: 可安装的 debug APK；真机不依赖电脑转发

`capacitor.config.ts`:

```ts
import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.zhaomu.app",
  appName: "朝暮",
  webDir: "dist",
  android: { allowMixedContent: false },
  plugins: {
    CapacitorHttp: { enabled: true },
  },
};

export default config;
```

`nativePostJson`：

```ts
import { Capacitor, CapacitorHttp } from "@capacitor/core";
import type { PostJson } from "./deepseek";

export const nativePostJson: PostJson = async (url, headers, body) => {
  try {
    const res = await CapacitorHttp.request({
      url,
      method: "POST",
      headers: { "Content-Type": "application/json", ...headers },
      data: body,
      connectTimeout: 30000,
      readTimeout: 60000,
    });
    return { status: res.status, json: res.data };
  } catch {
    return { status: 0, json: {} };
  }
};

export function activePostJson(): PostJson {
  return Capacitor.isNativePlatform() ? nativePostJson : browserPostJson;
}
```

设置存储：`Capacitor.isNativePlatform()` 时

```ts
import { Preferences } from "@capacitor/preferences";
const capPrefs: Prefs = {
  async get(key) {
    const { value } = await Preferences.get({ key });
    return value;
  },
  async set(key, value) {
    await Preferences.set({ key, value });
  },
};
```

Android 网络：`android/app/src/main/AndroidManifest.xml` 确认 `INTERNET`。`npx cap add android` 之后检查。明文 HTTP 不需要。

- [ ] **Step 1: 初始化 Capacitor 安卓工程**

Run:

```bash
npm run build
npx cap init 朝暮 com.zhaomu.app --web-dir dist
npx cap add android
npx cap sync android
```

Expected: 生成 `android/` 目录

若 `cap init` 与手写 `capacitor.config.ts` 冲突，保留手写 config，不要覆盖 appId。

- [ ] **Step 2: 实现 `activePostJson` 并接到 `complete`**

单测不跑原生。补一个纯函数测试：当传入的 `postJson` 被调用即视为接线正确（Task 6 已覆盖）。本步改 App 使用 `activePostJson()`。

- [ ] **Step 3: 打 debug APK**

Run:

```bash
npm run build
npx cap sync android
cd android && ./gradlew assembleDebug
```

Expected: `android/app/build/outputs/apk/debug/app-debug.apk`

电脑需 JDK 17 + Android SDK。没有的话安装 Android Studio，打开 `android/` 点 Run 到小米（USB 调试）。

- [ ] **Step 4: 真机验收（规格第 11 节）**

按顺序勾：

1. 侧载 APK，三页能进，不连电脑
2. 默认或设置 Key 能聊
3. 朝：报留下的任务、建议顺序、确认框、确认后待办可见
4. 待办能勾
5. 「还要给房东转水电费」弹框；「有点烦」不弹
6. 暮生成日记
7. 杀进程再开数据还在
8. 飞行模式能勾不能聊，Debug 看得到失败
9. 朝/暮只在对话页

- [ ] **Step 5: Commit**

```bash
git add capacitor.config.ts android src/api/post-json.ts src/storage/settings.ts src/App.vue
git commit -m "feat: wrap the app in Capacitor Android with native HTTP"
```

不要把 `app-debug.apk` 和 `local.properties` 提交进 git。

---

## Self-Review

**Spec coverage**

| 规格 | 任务 |
| --- | --- |
| 三页 + 朝暮仅对话 | Task 8 |
| 朝/暮/闲聊 + 确认才写 Todo | Task 7, 9 |
| 待办打卡、跨天 open 保留 | Task 4, 9 |
| 日记四段、覆盖当天 | Task 4, 7, 9 |
| 自己的 Key + 默认调试 Key | Task 2, 5, 9 |
| 屏幕 Debug 事件列表 | Task 3, 6, 7, 9 |
| 单机 APK、原生 HTTP | Task 10 |
| 不流式、不 thinking | Task 6, 7 |
| 401/无网/循环上限 | Task 6, 7, 9 |
| 无小组件/周月年/账号 | 全计划未引入 |

**Placeholder scan:** 无 TBD。默认 Key 允许为空字符串，作者本地填入。

**Type consistency:** `ProposedTodo`、`AgentDeps.onPropose`、`PostJson`、`chatCompletions`、`todoRepo.toggle` 名称在各任务一致。

---

## 执行说明

作者把 DeepSeek Key 填进 `src/debug/default-key.ts`（空模板已提交）后再打 APK。不要把填好的 Key commit 上去。
